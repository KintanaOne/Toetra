model := "linear.joblib"
target := score

max_score := 7.0
minimum_a := 0.0
maximum_a := 3.0

[BOUND]:
forall x0
    with domain(
        x0.a: [minimum_a, maximum_a]
    )
    => target <= max_score
    using Z3
